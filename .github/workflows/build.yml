name: Build CrystalDust

on:
  push:
    branches: [ "progrees" ]
  pull_request:
    branches: [ "progrees" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Host Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libpng-dev pkg-config git curl bzip2 automake autoconf libtool

      - name: Compile Repository Tools and agbcc
        run: |
          make tools
          if [ -d "tools/agbcc" ]; then
            cd tools/agbcc
            ./build.sh
            ./install.sh
            cd ../..
          fi

<<<<<<< HEAD
      - name: Setup agbcc Paths
        run: |
          echo "AGBCC=$PWD/tools/agbcc" >> $GITHUB_PATH
          echo "PATH=$PWD/tools/agbcc:$PATH" >> $GITHUB_ENV

      - name: Build ROM
        run: |
=======
      - name: Download and Setup ARM GCC 6
        run: |
          curl -L -o gcc-arm-none-eabi.tar.bz2 https://developer.arm.com/-/media/Files/downloads/gnu-rm/6-2017q2/gcc-arm-none-eabi-6-2017-q2-update-linux.tar.bz2
          tar -xf gcc-arm-none-eabi.tar.bz2
          echo "$PWD/gcc-arm-none-eabi-6-2017-q2-update/bin" >> $GITHUB_PATH
          echo "CPATH=$PWD/gcc-arm-none-eabi-6-2017-q2-update/lib/gcc/arm-none-eabi/6.3.1/include:$PWD/gcc-arm-none-eabi-6-2017-q2-update/arm-none-eabi/include" >> $GITHUB_ENV

      - name: Build ROM
        run: |
          arm-none-eabi-gcc --version
>>>>>>> d31bb7af4baeb3d346d0455271bb7c980a7ee88e
          make -j$(nproc)

      - name: Upload ROM Artifact
        uses: actions/upload-artifact@v4
        with:
          name: CrystalDust-ROM
          path: CrystalDust.gba